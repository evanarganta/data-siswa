## Running

- Clone the repository, or just copy the code. It's only one file anyway.
- Put it in your `\laragon\www\`, `\xampp\htdocs\`, or whatever directory your local server uses.
- Open your browser and go to `localhost/folder-name` or `localhost/folder-name/index.php`. Both should work.
